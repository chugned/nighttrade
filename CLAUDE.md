# Claude working notes — nighttrade

> Context preserved for future Claude sessions on this codebase. Read this
> first; it captures architectural decisions, conventions, and operational
> facts the repo doesn't otherwise document. Update as you make material
> changes — out-of-date notes are worse than no notes.

## What this is

US-equities (S&P 500) research and paper-trading observatory. Python 3.9,
FastAPI dashboard, SQLite observatory database, yfinance market feed.
Sibling project of `daytrade` (crypto) — shares most of its architecture.

**Paper trading only.** No live-execution scaffold exists in this repo
(unlike daytrade). Adding one would require porting `daytrade/src/.../live/`
and the same SafetyConfig opt-in pattern.

## Tool surface

The `Makefile` is the canonical entry point:

```
make test         # full pytest suite
make observe      # 24/7 live S&P 500 observer (under launchd in prod)
make dashboard    # FastAPI dashboard on :8001
make backtest     # one-shot backtest
make watchlist    # symbol screener
```

## Two-directory split (IMPORTANT)

Nighttrade lives at **two** paths on the host:

| Path | Role |
| --- | --- |
| `~/Desktop/coding/nighttrade/` | **Dev** — source of truth for code. Edit here. |
| `~/nighttrade/` | **Deployed** — where launchd runs the bot from. Source of truth for `artifacts/observatory.db`, `logs/nighttrade.log`. |

After editing code:
```bash
bash deploy/sync.sh        # rsyncs dev → deployed and reloads launchd
```

The bot under launchd writes to `~/nighttrade/artifacts/observatory.db`.
Read from there if you need to inspect state.

## Where state lives — DON'T edit these

| Path | What it holds |
| --- | --- |
| `~/nighttrade/artifacts/observatory.db` | Live SQLite of the running bot |
| `~/nighttrade/logs/nighttrade.log` | RotatingFileHandler (50MB × 5 cap) |
| `~/nighttrade/logs/com.nighttrade.*.{out,err}.log` | launchd captures |
| `~/nighttrade/data/now.json` | Current "what am I doing" status (atomic write) |

## launchd integration

Two LaunchAgents are installed:
- `com.nighttrade.observer` — the 24/7 observe loop (`KeepAlive=true`, auto-restart, `ThrottleInterval=20s`)
- `com.nighttrade.dashboard` — FastAPI on port 8001

Status: `launchctl list | grep nighttrade`
Restart: `launchctl kickstart -k gui/$(id -u)/com.nighttrade.observer`
Plists live in `~/Library/LaunchAgents/` and are generated by `deploy/install.sh`.

**Don't restart these without an explicit instruction.** The user's bot
state is in the running process.

## Architecture map

```
src/nighttrade/
├── observatory/
│   ├── observer.py     # the main loop (run_forever has backoff+abort)
│   ├── database.py     # SQLite schema + prune_old + bot-runs heartbeat semantics
│   ├── live_feed.py    # YFinanceFeed (threads=8 cap — see ADR-0005)
│   ├── alerts.py       # AlertManager → insert_alert (NOT insert_error)
│   ├── prediction_tracker.py
│   ├── safety_score.py, regime_gate.py, readiness.py, calibration.py
│   └── ...
├── ops/                # Ported from daytrade — instance_lock, reconciliation,
│                       # api_keys, order_ids, notify, remote_log
├── cross_section/      # Universe ranker + factor model (nighttrade-specific)
├── ml/                 # Predictive + meta models
├── features/, pipeline.py
├── dashboard/          # Port 8001
├── macro/              # Macro scenarios + Gemini adapter
└── market_hours.py     # US market open/close (regular + pre/post)
```

## Conventions

**TDD mandatory** (see `~/.claude/skills/test-driven-development`).
**No completion claims without `make test` evidence.**
**ADRs in `docs/adr/`** for changes to: strategy, safety gates, broker
adapters, observatory schema, public CLI flags.

**Strict pytest markers** (see `pyproject.toml`):
- `@pytest.mark.safety`
- `@pytest.mark.leakage`
- `@pytest.mark.integration`

## Recent load-bearing decisions (see `docs/adr/` for full context)

These were ported from daytrade because nighttrade had the same bugs:

- **0001**: `AlertManager` writes to `alerts` table, not `errors`.
- **0002**: `mark_dangling_runs_crashed` PID-liveness check + `heartbeat()`
  self-heal.
- **0003**: `db.recent_errors()` excludes `context LIKE 'alert:%'` by default.
- **0004**: `db.prune_old(days=30)` + day-rollover hook.
- **0005**: `YFinanceFeed._refresh` uses `threads=False` (sequential).
  503 symbols × `threads=True` exhausted the macOS thread limit at
  startup (`RuntimeError: can't start new thread`). `threads=8` was
  tried as a compromise but still crashed when host thread pressure
  was modest (2386/2784 user threads in use). Sequential fetch is
  ~90s vs ~40s — still well inside the 300s cycle interval.
  Pinned by `tests/test_live_feed_threads_invariant.py`.

Nighttrade-specific items (no daytrade analog):
- "no live data for X" from yfinance is caught as `ValueError` in
  `observer._observe_symbol`, logged once per cycle as a warning, does
  NOT write to the errors table.
- Per-symbol candle cache capped at `_MAX_CACHED_BARS_PER_SYMBOL = 500`
  to bound RAM growth on the 503-symbol universe.

## Anti-patterns — DO NOT reintroduce

These are bugs that were fixed once and have a regression test pinning
them. If a test in this list fires, **read the linked ADR before
"fixing" the test**.

| Anti-pattern | Why it's banned | Pinned by |
| --- | --- | --- |
| `yf.download(threads=True)` or `threads=N` for any N > 0 | One OS thread per ticker × 503 symbols → `RuntimeError: can't start new thread` whenever the host has modest concurrent thread pressure. ADR-0005. | `tests/test_live_feed_threads_invariant.py` |
| `ThreadPoolExecutor(max_workers=None)` over the symbol universe | Same family of bug as above — defaults to `os.cpu_count() * 5` workers, contributes to the thread-budget exhaustion. Use a hard small cap, or process sequentially. | (no pin yet — none in the codebase) |
| Writing to `alerts` data via `insert_error` | Pollutes the dashboard error counter with informational messages. ADR-0001. | covered by alert-routing tests |

If you need to add new parallelism, do it with a small explicit
worker count (≤4) AND add a regression test that pins the upper
bound.

## Operational facts

- Heartbeat: each cycle (~5 min). Heartbeat older than 600s → unhealthy.
- Memory profile: ~600 MB steady-state. Higher than daytrade because of
  503 symbols × yfinance/pandas overhead.
- Daily prune runs on day rollover.

## When you start a session

1. Read this file.
2. `make test` — establish current state.
3. Check mission control at `100.127.143.106:8002` for live bot health.
4. If you edit code, remember to `bash deploy/sync.sh` to deploy.
5. Don't restart the launchd services, don't edit plists, don't touch
   `~/nighttrade/artifacts/`.
